# 缓存穿透

## 查询一个根本不存在的值(数据库中也不存在)

解决思路: 使用布隆过滤器(判断某个值是否不存在), 或者干脆直接缓存一个NULL值在redis,防止数据库过载

# 缓存击穿

## 一个热点key(爆款商品，活动商品)在失效的瞬间，被大量访问

解决思路: 将热点key设置永不过期(后台异步更新), 使用互斥锁保证同时只有一个请求去数据库加载数据并缓存, 其他请求排队等着直接拿缓存就行

# 缓存雪崩

## 大量的key在同一时间内集中失效, 或者Redis服务整体宕机

解决思路: 过期时间加上随机值进行偏移, 防止同时过期; 宕机这个后续再补充下
